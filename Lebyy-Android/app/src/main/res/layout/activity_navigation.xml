<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:contentDescription="test-NavigationScreen">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/lebyy_bg"
        android:orientation="vertical">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/lebyy_bg_2"
            android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar"
            app:title="@string/navigation_title"
            app:titleTextColor="@color/lebyy_text" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/contentRoot"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                
                <TextView
                    android:id="@+id/lastDeepLink"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:contentDescription="test-LastDeepLink"
                    android:text="Last deep link: —"
                    android:textColor="@color/lebyy_muted"
                    android:textSize="15sp" />
                <TextView android:id="@+id/deepLinkHelp" android:layout_width="match_parent" android:layout_height="wrap_content"
                    android:contentDescription="test-DeepLinkHelp" android:text="Open via: lebyy://forms · lebyy://shop · lebyy://waits"
                    android:textColor="@color/lebyy_muted" android:textSize="12sp" android:layout_marginBottom="12dp"/>
                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/bottomTabs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:contentDescription="test-BottomTabs"
                    app:tabIndicatorColor="@color/lebyy_primary"
                    app:tabSelectedTextColor="@color/lebyy_primary"
                    app:tabTextColor="@color/lebyy_muted"/>
                <TextView android:id="@+id/tabContent" android:layout_width="match_parent" android:layout_height="160dp"
                    android:gravity="center" android:text="Home Tab" android:textColor="@color/lebyy_primary"
                    android:textSize="22sp" android:textStyle="bold" android:contentDescription="test-TabContent-Home"
                    android:background="@color/lebyy_surface" android:layout_marginTop="12dp"/>
                
                <TextView
                    android:id="@+id/selectedTab"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:contentDescription="test-SelectedTab"
                    android:text="Selected tab: Home"
                    android:textColor="@color/lebyy_accent"
                    android:textSize="15sp" />

            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/lebyy_surface"
        android:contentDescription="test-Menu-panel" />
</androidx.drawerlayout.widget.DrawerLayout>
